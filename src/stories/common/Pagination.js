/* eslint-disable no-console */
import * as React from 'react';
import { Pagination as MuiPagination } from '@mui/material';

const Pagination = (args) => <MuiPagination { ...args }/>;

export default Pagination;
